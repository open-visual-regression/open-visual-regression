# Examples

Deployment examples for self-hosting OVR against different infrastructure combinations.

- [`docker/`](./docker): `docker-compose.yml` for various combinations of bundled vs. external Postgres, Redis, and object storage.
