# 13 · /setup page

**Ready to archive** — fully implemented as part of c36-orpc-api. See that change for the actual implementation details (oRPC contracts + router + useServerAction pattern).

Original tasks described an outdated `actions.ts` pattern and are superseded.
