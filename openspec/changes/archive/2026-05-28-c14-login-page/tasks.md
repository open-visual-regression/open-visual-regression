# 14 · /login page (old)

**Ready to archive** — superseded by `14-login-page` which tracks actual implementation progress. This copy described an `actions.ts` pattern that was never used; login uses `authClient.signIn.email` client-side.
